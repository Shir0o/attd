/// How the attendance roster sorts during a marking session.
///
/// This is a **per-event preset** (configured in the event editor and asked once
/// on first attendance), not a live in-session toggle — see
/// `AttendanceRosterList` and the event editor.
enum RosterGrouping { byFamily, byStatus }
